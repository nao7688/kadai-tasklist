<h1>タスク一覧</h1>

<div>
  <ul>
  <% @tasks.each do |task| %>
  <li>
    <%= link_to task.id, task %>
    <%= task.content %>
  </li>
  <% end %>
  </ul>
</div>

<%= link_to 'タスクを新たに登録',new_task_path  %>